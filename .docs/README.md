# .docs — Dolce Vita

Repo-local documentation.

| Folder          | Purpose                                              |
| --------------- | ---------------------------------------------------- |
| `adrs/`         | Architecture Decision Records (numbered `0001-*.md`) |
| `architecture/` | System design notes, integration maps                |
| `delivery/`     | Delivery / launch notes                              |
| `features/`     | Per-feature design docs                              |
| `operations/`   | Runbooks, triage notes, on-call references           |

The single source of truth for project status is [`../spec.md`](../spec.md).
Detail docs live here so the spec can stay under 500 lines.
